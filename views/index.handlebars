<h1>Tasty Burgers</h1>

<h2>Burgers devoured!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have not been eaten!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="Undevoured">Have you eaten this?</label><br>
    <input type="radio" name="devoured" id="devoured" value="1" checked> Devoured!<br>
    <input type="radio" name="devoured" id="notEaten" value="0"> Not Eaten
  </div>

  <button type="submit">Add Burger</button>
</form>
